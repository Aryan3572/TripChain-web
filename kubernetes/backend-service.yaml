apiVersion: v1
kind: Service
metadata:
  name: tripchain-backend-service
spec:
  selector:
    app: tripchain-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: LoadBalancer
