apiVersion: v1
kind: Service
metadata:
  name: tripchain-frontend-service
spec:
  selector:
    app: tripchain-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
